# ===== Backend connection =====
# URL of the NestJS backend (WebSocket + HTTP).
# Leave empty to auto-detect using the browser's current hostname.
# In Docker Compose or for specific backend addresses, set this to http://host:3000
# VITE_WS_URL=http://localhost:3000
# VITE_WS_PORT=3000 (default)
